#ifndef TRIANGLE
#define TRIANGLE

#include <iostream>
#include <SDL.h>
#include <GL/glew.h>

void moving_triangle(float* triangle_coord, float* Move_pos, int size_list, int stride, int start_point);

#endif